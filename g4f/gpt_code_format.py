"""
Конечно! Вот пример кода на Python, который устанавливает будильник на полчаса:

```python
"""

import time
import winsound

# Устанавливаем время будильника
alarm_time = time.time() + 1800  # 1800 секунд = 30 минут

print("Будильник установлен на 30 минут.")

while True:
    current_time = time.time()
    if current_time >= alarm_time:
        print("Время будильника!")
        winsound.Beep(1000, 1000)  # Звук будильника
        break
    time.sleep(1)  # Проверяем каждую секунду


"""
```

Этот код устанавливает будильник на 30 минут и издает звуковой сигнал, когда время истекает. Убедитесь, что у вас установлен Python и библиотека `winsound` доступна на вашей системе Windows.
"""